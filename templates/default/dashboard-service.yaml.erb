apiVersion: v1
kind: Service
metadata:
  name: <%= @service_name %>
  namespace: kube-system
  labels:
    k8s-app: <%= @service_name %>
    kubernetes.io/cluster-service: "true"
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 9090
  selector:
   k8s-app: <%= @service_name %>
